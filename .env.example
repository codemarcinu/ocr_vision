# OpenWeatherMap (for weather queries in Chat AI)
# OPENWEATHER_API_KEY=your_openweather_api_key_here
# WEATHER_CITY=Kraków

# Ollama Configuration (optional - defaults shown)
# OLLAMA_BASE_URL=http://host.docker.internal:11434
# OCR_MODEL=qwen2.5vl:7b
# OCR_BACKEND=vision
# CLASSIFIER_MODEL=qwen2.5:7b

# Push Notifications - Web Push (optional)
# Generate keys: python scripts/generate_vapid_keys.py
# PUSH_ENABLED=false
# PUSH_VAPID_PUBLIC_KEY=
# PUSH_VAPID_PRIVATE_KEY=
# PUSH_VAPID_SUBJECT=mailto:admin@localhost

# Google Drive Sync (host-side rclone + in-container monitoring)
# Setup: ./scripts/setup-gdrive-sync.sh
# GDRIVE_SYNC_ENABLED=false
# FOLDER_WATCH_ENABLED=false
# FOLDER_WATCH_INTERVAL_SECONDS=300

# HTTPS via Caddy (optional)
# Start with: docker compose --profile https up -d
# CADDY_DOMAIN=localhost
